//Isso é um comentário de uma linha
/*Isso é um
código de várias linhas*/

var y = 0; //a criação de uma variável inicia com var

/*É uma boa prática você dar nomes que tenham sentido
a suas variáveis*/

var preco = 2;
var desconto = 0.2;
/*const PRECO = 2; uma constante é declarada com
letras maiúsculas, isso é uma boa prática.

O valor de uma constante não pode ser alterado.
Você não pode fazer PRECO = PRECO - desconto;

Mas eu tenho a possibilidade de fazer o seguinte:
var desconto = 0.2;
const PRECO = 2;
var total = PRECO - desconto;*/

preco = preco - desconto